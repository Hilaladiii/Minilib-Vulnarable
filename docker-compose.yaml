services:
  mysql:
    image: mysql:latest
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=root
    volumes:
      - minilibvulnarable-data:/var/lib/mysql

volumes:
  minilibvulnarable-data:
